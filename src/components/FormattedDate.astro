---
interface Props {
	date: Date
}

const { date } = Astro.props
---

<time datetime={date.toISOString()} class="text-sm">
	{
		date.toLocaleDateString("en-in", {
			year: "numeric",
			month: "short",
			day: "numeric",
		})
	}
</time>
